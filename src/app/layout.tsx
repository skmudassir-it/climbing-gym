import type { Metadata } from "next";
import "./globals.css";
import { Navbar } from "@/components/layout/Navbar";
import { Footer } from "@/components/layout/Footer";

export const metadata: Metadata = {
  title: {
    default: "SummitLine Climbing Gym — Scale New Heights",
    template: "%s | SummitLine Climbing Gym",
  },
  description:
    "SummitLine Climbing Gym offers world-class bouldering, top-rope, lead climbing, youth programs, and private coaching. Join our community and conquer every wall.",
  icons: { icon: "/favicon.svg" },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className="min-h-screen flex flex-col">
        <Navbar />
        <main className="flex-1">{children}</main>
        <Footer />
      </body>
    </html>
  );
}
